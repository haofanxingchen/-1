AI解释

一、两个前置概念
1. 自旋锁是不可重入的 PollSet 里那把锁是 spin::Mutex（自旋锁）。它 lock() 的原理是：发现锁被占用，就原地空转等待，直到释放。它不记录"是谁占的"——所以同一个线程如果已经持锁，再调一次 lock()，它会傻等"别人"释放，而这个"别人"就是它自己，永远等不到 → 死锁。这就是"不可重入"。

2. Waker::wake() 是"调用用户侧代码" wake() 不是简单置个标志，它会执行被唤醒任务注册的回调（可能重新 poll、重新 register、甚至调度器操作）。这是你无法控制、可能反过来碰你锁的代码。

二、出事的那几行（锁在哪）
// PollSet::register —— 锁在这里拿到
pub fn register(&self, waker: &Waker) {
    self.0.lock().register(waker);   // ① 拿锁，guard 一直活到本行结束
}

// Inner::register —— 在"还持着上面那把锁"的情况下执行
fn register(&mut self, waker: &Waker) {
    let slot = self.cursor % 64;
    if self.cursor >= 64 {            // ② 环满了，要驱逐
        let old = ...;                //    读出一个旧 waker
        if !old.will_wake(waker) {
            old.wake();               // ③ 持锁唤醒旧 waker  ← 罪魁祸首
        }
        ...
    }
    ...
}
三、死锁的完整调用链
假设某个 waker W 的 wake() 会重新访问同一个 PollSet（比如重新 register，或再 poll）：

环满了（64 个），W 正躺在 slot 0。
线程 T 第 65 次调用 register → self.0.lock()，T 拿到锁（①）。
cursor >= 64 成立，驱逐 slot 0：old = W，!will_wake 为真 → 执行 W.wake()（③）。
W 的 wake() 里又调 ps.register(...) → 又执行 self.0.lock()。
可是这把锁现在正被 T 自己握着。自旋锁不认识 T，它开始空转，等锁释放。
锁永远不可能释放——因为 T 卡在步骤 4 的空转里，回不到步骤 3 去 drop guard。
→ T 永远自旋，死锁。
一句话：在持锁状态下调用 wake()，而 wake() 又回过头来抢同一把锁，自己把自己锁死了。

四、对照：wake() 为什么是对的
同一个文件里，PollSet::wake 是正确示范——它先把 waker 换出来，释放锁，再唤醒：

pub fn wake(&self) -> usize {
    let mut guard = self.0.lock();
    if guard.is_empty() { return 0; }
    let inner = core::mem::replace(&mut *guard, Inner::new()); // 把 waker 整体取出
    drop(guard);        // ④ 先放锁
    inner.len()         // ⑤ 旧 inner 在这之后才被 drop → waker 才被唤醒（锁外）
}
所以这个 bug 的本质是不一致：wake() 做到了"锁外唤醒"，而 register() 的驱逐路径没做到。

五、我的测试为什么能抓到它
ReentrantWaker 就是个"wake() 会重新进 PollSet"的 waker，专门模拟步骤 4。修复前：第 65 次 register 触发死锁 → 测试 2 秒超时失败；修复后（驱逐的 waker 被取出来、在锁释放后再 wake）→ 正常完成，测试变绿。